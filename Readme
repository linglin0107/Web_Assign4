This depository is for web assign4, which use Nodejs, Mongodb, Expressjs, Angularjs to build a small REST app. 
The model is product with attributes(id, title, price, instock and photo.
The post, get, put and delecte method could be tested with postman.
